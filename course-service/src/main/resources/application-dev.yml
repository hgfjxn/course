course:
  service:
    version: 1.0.0
dubbo:
  application:
    id: course-service
    name: course-service
  protocol:
    id: dubbo
    name: dubbo
    port: 12345
  scan:
    basePackages: win.hgfdodo.course.service
  registry:
    id: my-registry
    address: N/A
    timeout: 60000
  consumer:
    timeout: 60000
  provider:
    timeout: 60000
thrift:
  type: win.hgfdodo.course.user.UserService.Client
  address: localhost
  port: 8080